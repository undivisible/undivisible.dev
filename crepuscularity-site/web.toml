[site]
name = "undivisible.dev (static)"
version = "0.1.0"
description = "Static landing built with crepus web from semitechnological/crepuscularity (branch web-v3)"

# Build (after installing `crepus` from crepuscularity web-v3):
#   ./scripts/build-static.sh
# Or: crepus web build --site crepuscularity-site -o dist/index.html
#     && cargo run --release -p site-tools --bin expand-site-links -- dist/index.html
