[site]
name = "undivisible.dev"
version = "0.1.0"
description = ".crepus + WASM via crepus web build (semitechnological/crepuscularity web-v3)"

# Dev:  crepus web serve --site crepuscularity-site
# Ship: crepus web build --site crepuscularity-site --out-dir dist
#
# site.json here is optional: SEO <title>/meta + CSS variables for the HTML shell only.
